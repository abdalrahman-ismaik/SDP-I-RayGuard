---
name: prototype-demo
description: Prepare and rehearse the early RayGuard GUI showcase or a full P1/P2 demonstration using the matching acceptance scope, real model evidence and student explain-back.
---

Use for the early GUI showcase, P1/P2 rehearsal or a milestone handover. Inputs: agreed advisor
acceptance, verified revision/environment, authorized representative inputs and real run manifests.
Paths below are relative to the repository root. Read `AGENTS.md`, `docs/status.md`, active
task, latest applicable meeting records, `docs/context.md` and `docs/architecture.md`.

1. Establish which demonstration is requested. T28's early laptop GUI can use the verified
   intended existing model before full P1 training/evaluation or P2 completion. A meeting's
   ready-model statement alone does not verify its pairing or execution. Full P1 still needs
   the agreed device/region/benign-modified pipeline; P2 needs externally justified cases and
   appropriate component evidence. Synthetic grouping is interface-only. Keep the GUI's
   ASAP/SLG timing and unresolved prototype deadlines distinct; consult current task dates.
2. Write and execute clean-start steps in a fresh environment with external artifacts configured.
   Show benign, threat and failure examples, thresholds/provenance and honest latency.
3. Label every recorded output with run ID; preserve a clearly marked recorded real-run fallback
   for compute failure. No canned predictions presented as live inference or proof of safety.
4. Have the assigned presenters/contributors explain data/task distinctions, code path, errors
   and limitations. The team has five students; no lead is assigned by this skill. Record actual
   contributions and rehearsal evidence in `docs/tasks.md` and `docs/progress.md`.
   Create a milestone tag only after verified acceptance.

Output: tested demo instructions, representative real outputs/run links, limitations, rehearsal
and fallback record. Verify the clean-start procedure and exact revision. Stop a readiness claim
if inputs, model execution or agreed acceptance is missing; report the blocked gate and next
action. Local demo preparation does not authorize publication, private-data sharing or paid jobs.
